// Reviewed production Public Lyrics directory. Keep this identical to the
// Dockerfile ARG default so sitemap generation and the Next.js client use the
// same source when NEXT_PUBLIC_LYRICS_BASE_URL is unset.
export const DEFAULT_PUBLIC_LYRICS_BASE_URL = "https://translation.exmeaning.com/files/translation/lyrics";
